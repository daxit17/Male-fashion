import { useSelector } from "react-redux";

const Islogin = () => {

    // const auth = useSelector(state => state.auth)

    // if (auth.user) {
    //     return true;
    // } else {
    //     return false;
    // }

}

export default Islogin;